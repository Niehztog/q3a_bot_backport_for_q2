// Default bot chat file for Q3 botlib backport to Quake II.
// Loaded by BotLoadChatFile() via be_ai_chat.c::BotLoadInitialChat().
//
// Minimal stub — just defines one empty chat section so the parser succeeds.

chat "defaultbot"
{
}
