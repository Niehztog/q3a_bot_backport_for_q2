// Quake II chat match configuration for Q3 botlib backport.
// Parsed by be_ai_chat.c::BotLoadMatchTemplates().
//
// Format per context block:
//   <context_int> { "match string" = (type, subtype); ... }
//
// Minimal stub: one empty context so the parser succeeds.

0
{
}
